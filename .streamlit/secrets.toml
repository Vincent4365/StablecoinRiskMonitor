[sanctions]
file_path = "data/sanctions/sanctioned_wallets.csv"

# BigQuery and service account credentials removed — this app is cloud-CSV only.
# If you need to re-enable BigQuery later, add a minimal [bigquery] block with
# `project_id` and set `enable_public_access = false` for public deployment.